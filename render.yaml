# Render部署配置文件
# 这个文件可以帮助Render更好地理解项目配置

services:
  - type: web
    name: pdf-to-ppt
    env: python
    pythonVersion: "3.10.0"
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app

